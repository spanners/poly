Polynomial calculator written in Scheme
=======================================

Compatible with the Racket Scheme interpreter

To run, type in a Linux console with Racket installed:

$racket -f poly.ss -i

This runs the tests and provides you with a prompt to interact with the 
calculator

The representation is shown through example in tests.ss
